# CP-01R Review — 2026-09-19

Status: CONDITIONAL PASS — FINAL RELEASE-GATE FIX REQUIRED

Reviewed head: `38a40f3369d3a322da2dc949447e2cda987ef9d3`

## Confirmed resolved

### R-01 Remote CI
Resolved.

GitHub Actions main run `35443467860` completed successfully. Job `quality` executed and passed checkout, Node setup, dependency install, typecheck, lint, unit/component tests, content/asset validation, production build and dependency audit.

The earlier pre-run failure was correctly documented as GitHub account billing/spending-limit state rather than repository code.

### R-02 Static start contract
Resolved.

`npm start` now serves the generated `out/` directory through the repository-owned static server instead of using `next start`.

### R-03 Robots behavior
Partially resolved.

LOCAL/PREVIEW explicitly disallow `/` and unit tests exist.

A final release-gate inconsistency remains: `getRuntimeConfig().isIndexable` becomes true whenever `NEXLABS_ENV=PRODUCTION`, even if `NEXT_PUBLIC_SITE_ORIGIN` is absent. Metadata/robots can therefore become indexable without a canonical production origin.

### R-04 Main governance
Resolved.

Active ruleset `Protect main` targets `refs/heads/main` and blocks deletion/non-fast-forward changes, requires pull-request flow, and requires the strict `quality` status check.

## Additional verified constraints
- repository remains private;
- zero-cost architecture preserved;
- no backend/auth/database added;
- no fabricated founder/contact/legal data introduced;
- workstation policy remains active;
- no UGAS or Blender dependency introduced.

## Final finding

### R-05 — MEDIUM — Production origin/indexing gate
Required behavior:
- LOCAL/PREVIEW: non-indexable;
- PRODUCTION without explicit valid origin: fail closed and never emit indexable metadata/robots;
- PRODUCTION with explicit valid HTTPS origin: may become indexable.

Required fix:
1. make indexability dependent on both production environment and explicit valid origin, or enforce an equivalent fail-closed contract;
2. add tests for PRODUCTION without origin;
3. add a release validation command such as `validate:release` that exercises `assertReleaseEnvironment`;
4. document production promotion must run the release gate;
5. keep ordinary local/CI development checks independent of a purchased domain.

## Decision
CP-01/CP-01R is technically strong and all previous blockers are resolved.

Final approval is withheld only for R-05 because canonical/indexing behavior is a production trust/SEO gate.

Required next package:
`CP-01F — Production Release Gate Finalization`.

After CP-01F passes, CP-02 is READY.
