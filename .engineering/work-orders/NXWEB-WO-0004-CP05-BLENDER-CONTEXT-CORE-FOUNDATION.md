# NXWEB-WO-0004-CP05-BLENDER-CONTEXT-CORE-FOUNDATION

Status: `COMPLETED`

## OBJECTIVE

Admit CP-05 as one governed GEF/HIVE increment and execute the deterministic Blender Context Core foundation through WO-B3D-006, stopping before animation, logo projection integration, LOD/GLB export and runtime 3D.

## CONTEXT/HIVE PREFLIGHT

This Work Order is bound to Context Lock `NXWEB-LOCK-0004-CP05-BLENDER-CONTEXT-CORE-FOUNDATION`, repository `KayzenRoot/nexlabs-web`, base `affd133d9f8650ab91d2f0dd5871220306a3bba0`, planning source `KayzenRoot/nexlabs-startup@b541e802472a3acc75a3a8ebd3818d33de8a316f`, GEF v1.0.0 and HIVE v1.0.0. Run `scripts/hive_prepare.py` without an override after this Work Order is active and bind the exact SHA-256 to the Context Lock and evidence.

## CANONICAL BASIS

Read the target-repository authority in the established order, then validate `.engineering/planning-snapshots/nexlabs-startup/b541e802472a3acc75a3a8ebd3818d33de8a316f/MANIFEST.json` and the cached BR-05, BR-11, BR-04, BR-12, BR-17 and BR-03 sources. Tracked Git, Project Brain, Source Hierarchy and this Work Order remain authoritative.

## SCOPE

- WO-B3D-001: deterministic master scene, units, collections, naming, render settings, metadata and hygiene report.
- WO-B3D-002: ten reusable BR-05 primitives with stable names, parameters and review sheet.
- WO-B3D-003: seven canonical material families, restrained Dark Mineral Laboratory hierarchy and web-approximation notes.
- WO-B3D-004: static Context Core assembly with neutral and branded silhouette passes; no selected-logo geometry.
- WO-B3D-005: sparse deterministic fragments, nodes and routing fields with explicit seeds and density controls.
- WO-B3D-006: hero, projection, macro and review cameras plus key/rim/environment lighting and HTML safe-area evidence.
- Produce reconstructable tracked source, manifests, validators, modest review renders and BR-12 provenance.

## OUT OF SCOPE

WO-B3D-007+, semantic animation, NX-C-02 geometry or projection, LOD variants, GLB/web export, static release pack, final acceptance, Three.js/R3F, BrandMark/favicon promotion, UGAS generation, paid services, deployment and domain work.

## BLENDER/MCP GATE

Before scene mutation, record the actual Blender executable version and prove the live add-on with `get_addon_status` and `get_scene_info`. One bounded restart/reconnect is allowed. If the live path is unavailable, stop with `BLENDER_MCP_REQUIRED_FOR_CP05`. Repository policy also requires explicit authorization to transition the active workstation from static-only to Blender production; do not override that policy inside CP-05.

## REPOSITORY AND DETERMINISM

Keep deterministic builders, validators, parameter sources, manifests and bounded review assets in a repo-native CP-05 source area. Keep the working `.blend` under an ignored repository-relative local-artifact directory and record only its relative path, size and SHA-256. Use explicit named seeds; require stable scene fingerprints without requiring byte-identical `.blend` files or GPU renders.

## SELECTED-LOGO BOUNDARY

Record `NX-C-02` r1 and SHA-256 `16daeac469520dbae6ba224dbd87bc8f07510c734940d35412248f32d2364165` as metadata dependency only. Do not modify `brand/logo/candidates/NX-C-02.svg`, `components/brand/BrandMark.tsx`, logo candidates or runtime wiring.

## ACCEPTANCE CRITERIA

- Work Order and Context Lock are exact-base/digest bound and HIVE-prepared.
- Cached BR source blobs match `MANIFEST.json`.
- Live Blender MCP is proven before scene work and workstation policy permits the production layer.
- WO-B3D-001 through 006 each have explicit auditable evidence.
- Ten primitives, seven materials, static neutral/branded compositions, deterministic data matter and target framing evidence exist.
- Scene/provenance manifest and review render package are complete without premature approval or release status.
- No CP-06+ work, logo integration, runtime 3D or BrandMark change is present.
- Local validation and protected hosted quality/Governance checks pass on one exact candidate head.

## TESTS

Run the governance/HIVE compile and tests, deterministic CP-05 validators, Blender background builder/check commands using the detected executable, `npm run check`, `npm run test:e2e`, `npm run security`, expected fail-closed `npm run validate:release`, and Wrangler dry-run. Do not claim a test that was not run on the exact candidate head.

## DELIVERABLES

Exact base/head and branch; Work Order/Context Lock digests; HIVE task and receipts; Blender version/add-on receipt; deterministic builder, validator and parameters; local `.blend` evidence without absolute paths; scene/primitive/material/assembly/data/camera-light manifests; review render package; BR-12 provenance; validation matrix; protected PR and exact-head hosted run IDs; explicit out-of-scope confirmation.

## REVIEW FORMAT

Return exactly one verdict: `APPROVED`, `CORRECTION REQUIRED` or `BLOCKED`. Report WO-B3D-001 through 006 separately, then Blender/MCP, deterministic scene, HIVE, local tests, hosted checks and carried-forward risks. Do not merge before independent review.

## STOP CONDITION

Stop only after WO-B3D-001 through 006, auditable manifests/renders, HIVE and local validation pass, the protected PR is open and exact-head quality/Governance are green. Do not merge and do not begin WO-B3D-007 or CP-06.

## EXECUTION REFERENCES

Source document SHA-256: `183ae223bb01fdac47f20ff1359657b63011e8a51ec3885f04f8b24e4e6bada1`.
Planning cache: `.engineering/planning-snapshots/nexlabs-startup/b541e802472a3acc75a3a8ebd3818d33de8a316f/`.
Frozen UGAS V1 source: `0169f84248703931bb7578177d9773bce319c14e`.
