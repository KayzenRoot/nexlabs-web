# Application environment: LOCAL, PREVIEW, or PRODUCTION.
NEXLABS_ENV=LOCAL

# Optional origin used for canonical URLs and Open Graph metadata.
# Leave empty for ordinary local builds. Production release validation requires it.
NEXT_PUBLIC_SITE_ORIGIN=
